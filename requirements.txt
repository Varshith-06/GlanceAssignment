--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.9.1
torchvision==0.24.1
transformers==4.57.1
chromadb==1.3.7
numpy==2.2.6
pillow==11.3.0
scikit-learn==1.7.2
scikit-image==0.25.2
pyyaml==6.0.3
matplotlib==3.10.7
pytest==8.4.2
tqdm==4.67.1
# Dependency parsing for query-side attribute binding (offline). The English
# model is a separate download:
#   python -m spacy download en_core_web_sm
# If it is missing, the parser degrades gracefully to the adjacency rule.
spacy==3.8.7
